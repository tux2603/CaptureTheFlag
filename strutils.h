#include <string>

// Counts the nuber of words in str
int countWords(char *str);
int countWords(std::string str);

// Returns an array holding all the words in the string
char **getWords(char *str);
std::string *getWords(std::string str);


